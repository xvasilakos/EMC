package stats.handlers;

/**
 * Same as ICompute1 and also requiring knowledge of the caching method in order to
 * compute statistics.
 *
 * @author xvas
 */
public interface IComputeGain<U, C> extends stats.handlers.ICompute {

   public abstract double compute2(U user) throws stats.StatisticException;
}
