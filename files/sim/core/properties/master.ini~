# This is the master ini file with references to the individual ini files used.
# Individual ini files are organised in the best possible way to reflect 
# relevance and coherence among parameters. 

## General simulation properties
include <defaultprops>/general.ini   	


## Caching stategies and related parameters
include <defaultprops>/caching.ini      


## Logging options
include <defaultprops>/logging.ini	


## Options for statistcs
include <defaultprops>/stats.ini	


## Properties for setting space options. For instance, area size and the dimensions of Smaller Cells (SCs), 
 # handover/mobile transition probability values and so forth.. 
include <defaultprops>/mobileMobilityModel.ini	


## Space related options: SCs creation, coverage
include <defaultprops>/space.&&.ini 		
# Options for Mobile Users (MU) 
include <defaultprops>/space.mu.&&.ini 		 
# Options for mobile demand 
include <defaultprops>/space.mu.dmd.&&.ini

## Cache and transmission costs							  					  
include <defaultprops>/costs.ini

## Networking properties such bit rates
include <defaultprops>/networking.ini	

## Define the number of simulation repeats via randomness seed numbers used in each simulation run.
include <defaultprops>/repeats.ini
